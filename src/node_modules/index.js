import run, { bindConsole, setContainer } from './lib/run';
import Input from './components/Input';

export { default as Console } from './components/Console';
export { run, bindConsole, setContainer, Input };
